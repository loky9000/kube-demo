apiVersion: v1
kind: Service
metadata:
  labels:
    name: nginx
  name: nginx
spec:
  type: NodePort
  clusterIP: 10.10.127.127
  ports:
    # the port that this service should serve on
    - targetPort: 80
      nodePort: 30752
  selector:
    name: nginx
